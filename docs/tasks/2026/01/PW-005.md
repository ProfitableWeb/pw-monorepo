# PW-005: Бекенд API и CI/CD для Cloud.ru

## 📋 Информация о задаче

- **ID**: PW-005
- **Статус**: DONE
- **Создано**: 2026-01-03
- **Приоритет**: High ⚡
- **Компонент**: 🐍 Backend | 🔧 DevOps
- **Теги**: #backend #api #cicd #cloudru #deployment #ssh

## 🎯 Постановка задачи

### Описание

Создать элементарный бекенд с приветствующей информацией об API и настроить CI/CD pipeline для автоматического деплоя на виртуальную машину в Cloud.ru.

### Цель

- Рабочий API endpoint с информацией о сервисе
- Автоматический деплой через CI/CD на VM в Cloud.ru
- Настроенный SSH доступ для деплоя

### ⚡ Быстрая инструкция: Настройка подключения

**1. Найти данные в Cloud.ru:**

- **IP адрес**: Консоль → VM → "Сетевые параметры" → "Публичный IP"
- **Пользователь**: Обычно `ubuntu` для Ubuntu 22.04 (проверить в "Настройки авторизации")
- **Пароль**: В "Настройки авторизации" или через виртуальную консоль

**2. Добавить SSH ключ на VM:**

- Через виртуальную консоль Cloud.ru (см. раздел 3 ниже)
- Или через `ssh-copy-id` с паролем

**3. Добавить secrets в CI/CD:**

- **GitVerse/GitLab**: Settings → CI/CD → Variables
- **GitHub**: Settings → Secrets and variables → Actions
- Добавить: `SSH_PRIVATE_KEY`, `SSH_HOST`, `SSH_USER`, `SSH_PORT`

### Критерии приемки

- [ ] API endpoint `/` возвращает информацию об API (название, версия, статус, endpoints)
- [ ] API endpoint `/docs` доступен (FastAPI автоматическая документация)
- [ ] CI/CD pipeline настроен (GitHub Actions / GitLab CI)
- [ ] SSH ключ сгенерирован и добавлен в secrets
- [ ] Деплой на VM работает автоматически при push в main
- [ ] API доступен по публичному IP/домену

## 🛠️ Техническая часть

### 1. Бекенд API

**Файл**: `apps/api/src/main.py`

- Улучшить endpoint `/` с детальной информацией:
  - Название API
  - Версия
  - Статус (operational)
  - Доступные endpoints
  - Документация (`/docs`, `/redoc`)
- Сохранить существующие endpoints (`/health`, `/api/articles`)

### 2. CI/CD Pipeline

**Вариант A: GitHub Actions** (если репозиторий на GitHub)

- Файл: `.github/workflows/deploy.yml` (пример создан)
- Триггер: push в `main` ветку
- Использует secrets: `SSH_PRIVATE_KEY`, `SSH_HOST`, `SSH_USER`, `SSH_PORT`

**Вариант B: GitLab CI** (если репозиторий на GitVerse/GitLab)

- Файл: `.gitlab-ci.yml` (пример создан)
- Использует CI/CD Variables: `SSH_PRIVATE_KEY`, `SSH_HOST`, `SSH_USER`, `SSH_PORT`
- Триггер: push в `main` ветку

### 3. SSH настройка

**Генерация ключа:**

```bash
ssh-keygen -t ed25519 -C "deploy@profitableweb" -f ~/.ssh/cloudru_deploy
```

**Где найти данные для подключения в Cloud.ru:**

1. **IP адрес VM:**
   - В консоли Cloud.ru: раздел "Сетевые параметры" → "Публичный IP"
   - Или в разделе "Информация" → "Сетевые интерфейсы"

2. **Имя пользователя:**
   - Для Ubuntu 22.04: обычно `ubuntu`
   - Может быть указано в разделе "Настройки авторизации"
   - Если не указано - проверить через виртуальную консоль Cloud.ru

3. **Пароль (только для первого подключения):**
   - В разделе "Настройки авторизации" → "Пароль"
   - Или установить через виртуальную консоль Cloud.ru

**Добавление SSH ключа на VM (первый раз):**

**Вариант A: Через виртуальную консоль Cloud.ru**

1. В консоли Cloud.ru: "Виртуальная консоль" → открыть
2. Войти с паролем (если есть) или создать пользователя
3. Выполнить:

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

**Вариант B: Через SSH с паролем (если пароль известен)**

```bash
# Скопировать публичный ключ на VM
ssh-copy-id -i ~/.ssh/cloudru_deploy.pub ubuntu@IP_АДРЕС_VM
# Или вручную:
cat ~/.ssh/cloudru_deploy.pub | ssh ubuntu@IP_АДРЕС_VM "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Проверка подключения:**

```bash
ssh -i ~/.ssh/cloudru_deploy ubuntu@IP_АДРЕС_VM
# Должно подключиться без пароля
```

**Secrets для CI/CD (GitVerse/GitLab):**

1. В репозитории: **Settings → CI/CD → Variables**
2. Добавить переменные:
   - `SSH_PRIVATE_KEY` - содержимое файла `~/.ssh/cloudru_deploy` (весь ключ, включая `-----BEGIN...` и `-----END...`)
   - `SSH_HOST` - IP адрес VM (например: `185.123.45.67`)
   - `SSH_USER` - имя пользователя (обычно `ubuntu`)
   - `SSH_PORT` - порт (обычно `22`)

**Secrets для CI/CD (GitHub Actions):**

1. В репозитории: **Settings → Secrets and variables → Actions → New repository secret**
2. Добавить секреты:
   - `SSH_PRIVATE_KEY` - содержимое `~/.ssh/cloudru_deploy`
   - `SSH_HOST` - IP адрес VM
   - `SSH_USER` - имя пользователя
   - `SSH_PORT` - порт (22)

### 4. Подготовка VM (один раз вручную)

**Базовые инструменты:**

**Архитектура на VM:**

```
VM Cloud.ru (213.171.25.187)
├── Nginx (порт 80/443) — reverse proxy
├── API (порт 8000) — Python/FastAPI
├── Web (порт 3000) — Next.js
└── Admin (порт 3001) — Next.js
```

**Шаг 1: Базовые пакеты**

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl wget nginx
```

**Шаг 2: Python 3.11 + uv (для API)**

```bash
sudo apt install -y python3.11 python3.11-venv
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
```

**Шаг 3: Bun (для Next.js web + admin)**

```bash
curl -fsSL https://bun.sh/install | bash
source ~/.bashrc
```

**Шаг 4: PM2 (менеджер процессов)**

```bash
bun install -g pm2
```

**Проверка:**

```bash
git --version && python3.11 --version && uv --version && bun --version && pm2 --version && nginx -v
```

### 5. Nginx конфиг

**Файл:** `/etc/nginx/sites-available/profitableweb`

```nginx
server {
    listen 80;
    server_name profitableweb.ru www.profitableweb.ru;

    # API
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Admin panel
    location /admin {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Frontend (всё остальное)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 6. PM2 ecosystem

**Файл:** `ecosystem.config.js` (в корне репозитория)

```javascript
module.exports = {
  apps: [
    {
      name: "api",
      cwd: "./apps/api",
      script: "uv",
      args: "run uvicorn src.main:app --host 127.0.0.1 --port 8000",
      interpreter: "none",
    },
    {
      name: "web",
      cwd: "./apps/web",
      script: "bun",
      args: "run start",
      interpreter: "none",
    },
    {
      name: "admin",
      cwd: "./apps/admin",
      script: "bun",
      args: "run start -- -p 3001",
      interpreter: "none",
    },
  ],
};
```

### 7. Деплой скрипт

**Процесс деплоя:**

```bash
cd ~/profitableweb
git pull origin master

# API
cd apps/api && uv sync

# Frontend
cd ../web && bun install && bun run build

# Admin
cd ../admin && bun install && bun run build

# Перезапуск всех сервисов
cd ~/profitableweb
pm2 restart ecosystem.config.js
```

### 8. Systemd сервис (альтернатива PM2)

**Файл**: `/etc/systemd/system/profitableweb-api.service`

```ini
[Unit]
Description=ProfitableWeb API
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/profitableweb-api/apps/api
ExecStart=/usr/local/bin/uvicorn src.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

## ✅ Чеклист выполнения

### TODO → DOING

- [x] Задача проанализирована
- [x] Выбран CI/CD провайдер (GitVerse/GitLab CI)
- [x] VM в Cloud.ru создана и доступна
- [x] SSH доступ к VM настроен локально ✅

### DOING → TESTING

**Бекенд:**

- [x] Endpoint `/` улучшен с информацией об API
- [x] Endpoint `/docs` доступен
- [x] Локально API работает (`uvicorn src.main:app`)

**SSH:**

- [x] SSH ключ сгенерирован
- [x] Публичный ключ добавлен на VM (Cloud.ru добавил автоматически)
- [x] Подключение по SSH работает без пароля ✅

**Подготовка VM (один раз вручную):**

- [x] Базовые пакеты: git, curl, wget, nginx, unzip ✅
- [x] Python 3.11 ✅
- [x] uv 0.9.21 ✅
- [x] Bun 1.3.5 ✅
- [x] Node.js 20.19.6 (для PM2) ✅
- [x] PM2 6.0.14 ✅
- [x] Nginx конфиг настроен

**CI/CD:**

- [x] Pipeline файл создан (`.gitlab-ci.yml`)
- [x] Secrets добавлены в CI/CD систему (SSH_PRIVATE_KEY, SSH_HOST, SSH_USER, SSH_PORT)
- [x] Pipeline проходит успешно (dry-run)

**Деплой:**

- [x] Деплой скрипт/команды работают
- [x] API доступен на VM после деплоя
- [x] Автоматический деплой при push работает

### TESTING → CODEREVIEW & DOCS

- [x] API отвечает на все endpoints
- [x] Документация `/docs` открывается
- [x] Деплой работает стабильно
- [x] Логи деплоя проверены
- [x] README обновлен с инструкциями по деплою

### CODEREVIEW & DOCS → DONE

- [x] Код соответствует стандартам
- [x] Secrets не закоммичены в репозиторий
- [x] Документация обновлена
- [x] Инструкции по настройке SSH добавлены

## 🔗 Связанные задачи

- **Блокируется**: PW-003 (Миграция на монорепо) ✅
- **Связана с**: Будущая задача "Настройка домена и SSL"

## 📝 Заметки и комментарии

### История изменений

- 2026-01-04: SSH подключение к VM работает (webresearcher@213.171.25.187)
- 2026-01-04: VM создана в Cloud.ru, SSH ключ сгенерирован, secrets добавлены в GitVerse
- 2026-01-03: Задача создана

### Детали Cloud.ru

- **Тип**: Виртуальная машина
- **ОС**: Ubuntu 22.04 LTS (рекомендуется)
- **Порты**: Открыть 8000 для API (или настроить reverse proxy)
- **Firewall**: Настроить правила в Cloud.ru панели

### Безопасность

- ⚠️ SSH ключ хранить только в secrets CI/CD
- ⚠️ Не коммитить приватные ключи
- ⚠️ Использовать отдельного пользователя для деплоя (не root)
- ⚠️ Настроить firewall на VM
- ⚠️ Рассмотреть reverse proxy (nginx) для production

### Альтернативы

- Если Cloud.ru не поддерживает прямое SSH - использовать их API для деплоя
- Рассмотреть Docker контейнеризацию для упрощения деплоя
- Использовать Ansible для автоматизации настройки VM

---

**Статусы**: TODO → DOING → TESTING → CODEREVIEW & DOCS → DONE
