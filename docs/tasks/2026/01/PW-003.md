# PW-003: Миграция проекта на Turborepo монорепо

## 📋 Информация о задаче

- **ID**: PW-003
- **Статус**: TODO
- **Создано**: 2026-01-03
- **Приоритет**: High ⚡
- **Компонент**: 🔧 DevOps | ⚛️ Frontend | 📚 Docs
- **Теги**: #architecture #monorepo #turborepo #migration #refactoring

## 🎯 Постановка задачи

### Описание

Мигрировать текущую мультирепозиторную структуру ProfitableWeb на Turborepo монорепо для улучшения developer experience, упрощения управления зависимостями и обеспечения атомарности изменений.

**Текущее состояние:**

- ✅ `profitable-web-frontend/` - готов на ~60% (главная страница, инфраструктура)
- 📋 `profitable-web-backend/` - только структура, без кода
- 📋 `profitable-web-admin/` - только структура, без кода
- 📚 `profitable-web-docs/` - документация

### Цель

Создать единую монорепозиторную структуру с:

- Общими TypeScript типами между Frontend/Admin/Backend
- Единой системой сборки через Turborepo
- Общими конфигурациями (ESLint, Prettier, TypeScript)
- Кэшированием сборок для ускорения разработки
- Атомарными коммитами для связанных изменений

### Критерии приемки

- [ ] Создан корневой монорепозиторий с Turborepo
- [ ] Frontend перенесён в `apps/web/` с сохранением работоспособности
- [ ] Создана структура для Backend (`apps/api/`) и Admin (`apps/admin/`)
- [ ] Создан `packages/types/` для общих TypeScript типов
- [ ] Создан `packages/config/` для общих конфигураций
- [ ] Настроен `turbo.json` с правильным pipeline
- [ ] Docs перенесена в корневую папку `docs/`
- [ ] Git история сохранена (через subtree или fresh start)
- [ ] Все npm скрипты работают через `turbo`
- [ ] Frontend собирается и запускается без ошибок
- [ ] Обновлена документация с новой структурой
- [ ] Репозиторий создан на GitVerse в организации profitableweb.ru
- [ ] Код запушен в GitVerse remote

## 🛠️ Техническая часть

### Компоненты для создания

```
ProfitableWeb/                    # Root монорепо
├── apps/
│   ├── web/                     # Frontend (перенос profitable-web-frontend)
│   │   ├── src/
│   │   ├── public/
│   │   ├── package.json
│   │   └── next.config.js
│   │
│   ├── admin/                   # Admin panel (структура)
│   │   ├── src/
│   │   ├── package.json
│   │   └── next.config.js
│   │
│   └── api/                     # Backend (структура)
│       ├── src/
│       ├── pyproject.toml
│       └── README.md
│
├── packages/
│   ├── types/                   # Shared TypeScript types
│   │   ├── article.ts
│   │   ├── api.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── ui/                      # Shared UI components (опционально)
│   │   ├── components/
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── config/                  # Shared configs
│       ├── eslint/
│       ├── prettier/
│       └── typescript/
│
├── docs/                        # Документация (перенос profitable-web-docs)
│   ├── PROJECT_OVERVIEW.md
│   ├── tasks/
│   └── architecture/
│
├── turbo.json                   # Turborepo configuration
├── package.json                 # Root package.json
├── bun.lock                     # Unified lockfile
├── .gitignore                   # Root gitignore
└── README.md                    # Root README
```

### Зависимости

- [x] Bun установлен (используется на frontend)
- [x] Git история frontend сохранена
- [x] GitVerse аккаунт и организация profitableweb.ru созданы
- [x] Новый репозиторий создан на GitVerse: https://gitverse.ru/profitableweb.ru/pw-monorepo
- [x] SSH ключ настроен для GitVerse (id_rsa_gitverse)
- [x] SSH подключение к GitVerse проверено и работает
- [ ] Git config настроен (user.name, user.email)
- [ ] uv установлен для Python (https://docs.astral.sh/uv/)
- [ ] Turborepo CLI установлен (bunx create-turbo)
- [ ] Новый Git репозиторий создан локально

### Предложенное решение

#### Этап 0: Настройка Git config (~2 минуты)

```bash
# Настроить Git для работы с GitVerse
git config --local user.name "e-nicko"
git config --local user.email "e-nicko@ya.ru"

# Проверить настройки
git config --local --list
```

#### Этап 1: Создание базовой структуры Turborepo (~15 минут)

```bash
# 1. Создать новую директорию для монорепо
mkdir ProfitableWeb-mono
cd ProfitableWeb-mono

# 2. Инициализировать Turborepo
bunx create-turbo@latest --use-bun

# 3. Очистить примеры и создать структуру
rm -rf apps/* packages/*
mkdir -p apps/web apps/admin apps/api
mkdir -p packages/types packages/config packages/ui
mkdir -p docs
```

#### Этап 2: Перенос Frontend (~20 минут)

```bash
# 1. Копировать Frontend с сохранением структуры
cp -r ../ProfitableWeb/profitable-web-frontend/* ./apps/web/

# 2. Обновить package.json в apps/web
# Добавить workspace dependencies: "@profitable-web/types": "workspace:*"

# 3. Переместить .git историю (опционально)
# git subtree add --prefix=apps/web ...
```

#### Этап 3: Создание shared packages (~30 минут)

```typescript
// packages/types/package.json
{
  "name": "@profitable-web/types",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts"
}

// packages/types/article.ts
export interface Article {
  id: string;
  title: string;
  slug: string;
  content: string;
  excerpt: string;
  category: string;
  tags: string[];
  publishedAt: Date;
  updatedAt: Date;
}

// packages/types/index.ts
export * from './article';
export * from './api';
export * from './category';
```

#### Этап 4: Настройка Turbo pipeline (~15 минут)

```json
// turbo.json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "out/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "type-check": {
      "dependsOn": ["^build"]
    }
  }
}

// Примечание: Python backend (apps/api) интегрирован через package.json shim
// Turborepo будет управлять им как обычным Node.js приложением
// Команды типа "turbo run dev" запустят "uv run uvicorn..." в apps/api
```

#### Этап 5: Создание root package.json (~10 минут)

```json
{
  "name": "profitable-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.6.2"
  },
  "packageManager": "bun@1.2.21"
}
```

#### Этап 6: Перенос документации (~5 минут)

```bash
# Копировать docs с сохранением структуры
cp -r ../ProfitableWeb/profitable-web-docs/* ./docs/

# Обновить пути в документации при необходимости
```

#### Этап 7: Создание структур для Backend и Admin (~15 минут)

````bash
# Backend structure
mkdir -p apps/api/src

# Backend package.json (shim для Turborepo)
cat > apps/api/package.json << 'EOF'
{
  "name": "@profitable-web/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload",
    "build": "echo 'Building Python API...'",
    "lint": "uv run ruff check .",
    "test": "uv run pytest",
    "type-check": "uv run mypy ."
  }
}
EOF

# Backend pyproject.toml
cat > apps/api/pyproject.toml << 'EOF'
[project]
name = "profitable-web-api"
version = "0.1.0"
description = "FastAPI backend for ProfitableWeb Research Lab"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "sqlalchemy>=2.0.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
EOF

# Backend README
cat > apps/api/README.md << 'EOF'
# ProfitableWeb API

FastAPI backend for ProfitableWeb Research Lab.

## Development

```bash
# Install dependencies
uv sync

# Run dev server
uv run uvicorn main:app --reload
````

EOF

# Admin structure

mkdir -p apps/admin/src/app
touch apps/admin/package.json
touch apps/admin/next.config.js

````

#### Этап 8: Git инициализация и push в GitVerse (~10 минут)

```bash
# 1. Инициализация Git
git init
git add .
git commit -m "feat: migrate to Turborepo monorepo structure

- Migrate frontend to apps/web
- Create shared packages (types, config)
- Setup Turborepo pipeline
- Add backend and admin app structures
- Migrate docs to root"

# 2. Репозиторий уже создан на GitVerse ✅
# https://gitverse.ru/profitableweb.ru/pw-monorepo
# Название: pw-monorepo
# Публичный репозиторий

# 3. Добавить GitVerse remote и запушить
git remote add origin https://gitverse.ru/profitableweb.ru/pw-monorepo.git
git branch -M master
git push -u origin master
````

## ✅ Чеклист выполнения

### TODO → DOING

- [x] Задача проанализирована
- [x] План миграции утверждён
- [x] GitVerse репозиторий создан
- [x] SSH доступ к GitVerse настроен
- [ ] Git config настроен (user.name, user.email)
- [ ] Backup текущего состояния создан
- [ ] Turborepo CLI установлен

### DOING → TESTING

- [ ] Git config настроен в новом монорепо
- [ ] Базовая структура Turborepo создана
- [ ] Frontend перенесён в apps/web/
- [ ] packages/types создан с базовыми типами
- [ ] packages/config создан с общими конфигами
- [ ] turbo.json настроен
- [ ] Root package.json создан
- [ ] Документация перенесена
- [ ] Структуры Backend/Admin созданы

### TESTING → CODEREVIEW & DOCS

- [ ] `bun install` выполняется без ошибок
- [ ] `bun turbo dev` запускает Frontend
- [ ] `bun turbo build` собирает Frontend
- [ ] `bun turbo lint` проверяет код
- [ ] `bun turbo test` запускает тесты
- [ ] Shared types импортируются корректно
- [ ] Workspace dependencies резолвятся

### CODEREVIEW & DOCS → DONE

- [ ] Вся функциональность Frontend работает
- [ ] CLAUDE.md обновлён с новой структурой
- [ ] PROJECT_OVERVIEW.md обновлён
- [ ] README.md создан в корне
- [ ] Git repository инициализирован
- [ ] Репозиторий создан на GitVerse (https://gitverse.ru/profitableweb.ru/)
- [ ] Код запушен в GitVerse remote
- [ ] Старые репозитории заархивированы

## 🔗 Связанные задачи

- **Блокируется**: PW-000 (Настройка проекта) ✅
- **Блокируется**: PW-001 (Проверка репозиториев) ✅
- **Блокирует**: PW-004 (Backend API разработка)
- **Блокирует**: PW-005 (Admin panel разработка)
- **Связана с**: Все будущие задачи разработки

## 📝 Заметки и комментарии

### История изменений

- 2026-01-03: Задача создана после анализа текущей структуры проекта
- 2026-01-03: Добавлен GitVerse как Git-хостинг (организация profitableweb.ru готова)
- 2026-01-03: Создан репозиторий на GitVerse: https://gitverse.ru/profitableweb.ru/pw-monorepo
- 2026-01-03: SSH ключ для GitVerse создан и настроен (id_rsa_gitverse), подключение проверено
- 2026-01-03: Обновлён подход к Python backend - добавлен package.json shim для интеграции с Turborepo

### Дополнительная информация

**Почему Turborepo:**

- ⚡ Rust-powered кэширование (совместимо с Bun философией)
- 🔗 Shared types между Frontend/Admin/Backend без дублирования
- 📦 Workspace dependencies - единая версионирование зависимостей
- ⚛️ Атомарные коммиты - одно изменение API = один коммит
- 🚀 Параллельная сборка и тестирование всех приложений

**Текущее состояние Frontend:**

- Готовность: ~30% (главная страница, инфраструктура)
- Роуты: только `/` (главная)
- Компоненты: Header, HeadContent
- Отсутствуют: `[slug]`, `categories/[category]`, `about`, большинство компонентов

**Идеальный момент для миграции:**

- ✅ Инфраструктура Frontend готова - легко переносить
- ✅ Мало страниц и компонентов - минимум конфликтов
- ✅ Backend не начат - можно сразу интегрировать правильно
- ✅ Admin не начат - создастся уже в монорепо

**Риски:**

- ⚠️ Возможны проблемы с path aliases при переносе
- ⚠️ Bun workspace support может иметь нюансы

**Почему Python backend:**

Python выбран для агентских AI-систем (LangChain, Pydantic AI, FastAPI). Интеграция в Turborepo через package.json shim. Внутри `apps/api` - чистый Python без компромиссов.

**Python в Turborepo:**

Turborepo заточен под Node.js, но через **package.json shim** мы превращаем его в агностичный оркестратор:

1. В `apps/api/package.json` прописываем npm scripts, которые вызывают `uv`
2. Turborepo видит backend как обычное Node.js приложение
3. Команды `turbo run dev` запускают `uv run uvicorn` автоматически
4. **Zero Friction** разработка - одна команда для всего стека

**Пример:**

```bash
# В корне монорепо - запустит ВСЁ (web, admin, api)
bun turbo dev

# Turbo сам пойдёт в apps/api и запустит: uv run uvicorn main:app --reload
```

**Альтернативы рассмотренные:**

1. Остаться на мультирепо - отклонено (боль с типами)
2. Гибридный подход (Frontend+Admin в моно, Backend отдельно) - возможно
3. Full monorepo с Turborepo - **выбрано**

**Git хостинг:**

- 🇷🇺 **GitVerse** - российская платформа Git-хостинга
- Организация: `profitableweb.ru` ✅
- Репозиторий: `pw-monorepo` ✅
- SSH ключ: `id_rsa_gitverse` настроен и проверен ✅
- Публичный репозиторий для открытого исследовательского проекта
- URL: https://gitverse.ru/profitableweb.ru/pw-monorepo

**Полезные ссылки:**

- [Turborepo Documentation](https://turbo.build/repo/docs)
- [Bun Workspaces](https://bun.sh/docs/install/workspaces)
- [Next.js Monorepo](https://nextjs.org/docs/advanced-features/multi-zones)
- [GitVerse](https://gitverse.ru/)

---

**Статусы**: TODO → DOING → TESTING → CODEREVIEW & DOCS → DONE
