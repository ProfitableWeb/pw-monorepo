# PW-016: Разработка универсального компонента Input

## 📋 Информация о задаче

- **ID**: PW-016
- **Статус**: ✅ DONE
- **Создано**: 2026-01-20
- **Приоритет**: Normal 📋
- **Компонент**: ⚛️ Frontend
- **Теги**: #frontend #component #input #form #ui
- **Оценка времени**: ~4-6 часов
- **Фактическое время**: ~2 часа

## 🎯 Постановка задачи

### Описание

Разработать универсальный компонент `Input` для использования во всех формах проекта. Компонент должен быть
переиспользуемым, доступным, поддерживать различные типы полей (email, text, password, tel и т.д.), состояния (error,
disabled, loading) и иметь единый стиль, соответствующий дизайн-системе проекта.

**Первичное использование:**

- Замена нативного `<input>` в компоненте `NewsletterForm` (модальное окно подписки на рассылку)
- Будущее использование в формах авторизации, обратной связи, комментариев и других формах проекта

### Цель

Создать универсальный компонент ввода, который:

- Обеспечивает единообразный UI/UX для всех форм
- Поддерживает все стандартные HTML input атрибуты
- Имеет встроенную поддержку состояний (error, disabled, loading)
- Соответствует дизайн-системе проекта (темная/светлая тема)
- Полностью доступен (accessibility)
- Легко расширяется и кастомизируется

### Критерии приемки

#### Функциональность

- [x] Компонент поддерживает все стандартные типы input: `text`, `email`, `password`, `tel`, `number`, `url`, `search`
- [x] Поддержка состояний:
  - [x] `error` - отображение ошибки валидации
  - [x] `disabled` - неактивное состояние
  - [ ] `loading` - состояние загрузки (опционально, для асинхронной валидации)
- [x] Поддержка `placeholder`, `value`, `defaultValue`
- [x] Поддержка всех стандартных HTML атрибутов (`required`, `maxLength`, `minLength`, `pattern`, `autocomplete` и т.д.)
- [x] Поддержка `ref` для доступа к нативному input элементу
- [x] Поддержка `onChange`, `onBlur`, `onFocus`, `onKeyDown` и других стандартных событий
- [x] Визуальная индикация фокуса (focus state)
- [x] Визуальная индикация ошибки (error message под полем)

#### Дизайн и стилизация

- [x] Стили соответствуют дизайн-системе проекта
- [x] Поддержка темной и светлой темы
- [x] Адаптивность (mobile, tablet, desktop)
- [x] Плавные переходы (transitions) для состояний
- [x] Стилизация через SCSS (как в других компонентах проекта)
- [x] Использование CSS переменных для цветов (из темы)

#### Accessibility

- [x] Правильная связь `label` с `input` через `id` и `htmlFor`
- [x] Поддержка `aria-label`, `aria-describedby`, `aria-invalid`
- [x] Сообщения об ошибках связаны с полем через `aria-describedby`
- [x] Keyboard navigation работает корректно
- [x] Focus visible для клавиатурной навигации
- [x] Соответствие WCAG 2.1 Level AA

#### Качество кода

- [x] TypeScript типы полностью определены
- [x] Компонент следует архитектуре проекта (как `Button`)
- [x] Структура файлов: `Input.tsx`, `Input.scss`, `index.ts`
- [x] Экспорт через `index.ts`
- [x] JSDoc комментарии для публичного API
- [x] Примеры использования в комментариях

#### Интеграция

- [x] Замена нативного `<input>` в `NewsletterForm` на новый компонент `Input`
- [x] Компонент работает корректно в модальном окне
- [x] Валидация email в `NewsletterForm` работает с новым компонентом
- [x] Нет регрессий в функциональности

### Зависимости

- [x] Существующий компонент `Button` (как референс архитектуры)
- [x] Существующий компонент `NewsletterForm` (для интеграции)
- [x] Система тем проекта (dark/light theme)
- [ ] Дизайн-макеты (опционально, можно использовать стиль существующих компонентов)

### Предложенное решение

#### 1. Структура компонента

```
apps/web/src/components/common/input/
├── Input.tsx          # Основной компонент
├── Input.scss         # Стили компонента
└── index.ts           # Экспорт
```

#### 2. API компонента (интерфейс)

```typescript
interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  // Дополнительные пропсы
  error?: string | boolean; // Сообщение об ошибке или флаг ошибки
  label?: string; // Текст label (опционально)
  helperText?: string; // Вспомогательный текст под полем
  fullWidth?: boolean; // Полная ширина контейнера
  className?: string; // Дополнительные классы
  // Стандартные HTML атрибуты наследуются от InputHTMLAttributes
}
```

#### 3. Примеры использования

```tsx
// Базовое использование
<Input
  type="email"
  placeholder="your@email.com"
  value={email}
  onChange={(e) => setEmail(e.target.value)}
/>

// С label и ошибкой
<Input
  type="email"
  label="Email адрес"
  value={email}
  onChange={(e) => setEmail(e.target.value)}
  error="Пожалуйста, введите корректный email"
  required
/>

// С helper text
<Input
  type="password"
  label="Пароль"
  helperText="Минимум 8 символов"
  value={password}
  onChange={(e) => setPassword(e.target.value)}
/>

// Disabled состояние
<Input
  type="text"
  value={value}
  disabled
/>

// В форме (NewsletterForm)
<Input
  type="email"
  value={email}
  onChange={(e) => setEmail(e.target.value)}
  placeholder="your@email.com"
  disabled={isLoading || status === 'success'}
  error={errorMessage || undefined}
/>
```

#### 4. Стилизация и поддержка тем

**Использование CSS переменных (автоматическая поддержка тем):**

Компонент `Input` должен использовать существующие CSS переменные из темы проекта для автоматической поддержки тёмной и
светлой темы без JavaScript кода.

```scss
// Input.scss - пример реализации
@import '@/styles/utils/variables';
@import '@/styles/utils/mixins';
@import '@/styles/utils/breakpoints';

.input {
  // Базовые стили
  display: block;
  width: 100%;
  padding: 14px 16px;
  font-size: 16px;
  line-height: 1.5;
  border-radius: 8px;

  // CSS переменные для автоматической поддержки тем
  background: var(--input-background); // #262626 dark, #fafafa light
  border: 2px solid var(--input-border); // #525252 dark, #d4d4d4 light
  color: var(--input-text); // #f5f5f5 dark, #171717 light

  // Placeholder
  &::placeholder {
    color: var(--input-placeholder); // #737373 для обеих тем
  }

  // Focus state
  &:focus {
    outline: none;
    border-color: var(--input-border-focus); // #5ADC5A (зеленый для обеих тем)
    box-shadow: 0 0 0 2px rgba(90, 220, 90, 0.1);
  }

  // Error state
  &--error {
    border-color: var(--color-error); // #f87171 dark, #ef4444 light
  }

  &--error:focus {
    box-shadow: 0 0 0 2px var(--color-error-light); // #7f1d1d dark, #fee2e2 light
  }

  // Disabled state
  &:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
}

// Error message text
.input__error-message {
  margin-top: 8px;
  font-size: 13px;
  color: var(--color-error); // Автоматически меняется с темой
}

// Helper text
.input__helper-text {
  margin-top: 8px;
  font-size: 13px;
  color: var(--color-text-muted); // Автоматически меняется с темой
}
```

**Переменные для форм (уже определены в проекте):**

```scss
// Из themes/dark/components/_forms.scss
[data-theme='dark'] {
  --input-background: #262626;
  --input-border: #525252;
  --input-border-focus: #5adc5a;
  --input-text: #f5f5f5;
  --input-placeholder: #737373;
}

// Из themes/light/components/_forms.scss
[data-theme='light'],
:root {
  --input-background: #fafafa;
  --input-border: #d4d4d4;
  --input-border-focus: #5adc5a;
  --input-text: #171717;
  --input-placeholder: #737373;
}

// Для ошибок - из _status.scss
[data-theme='dark'] {
  --color-error: #f87171;
  --color-error-light: #7f1d1d;
}

[data-theme='light'],
:root {
  --color-error: #ef4444;
  --color-error-light: #fee2e2;
}
```

**Поддержка темной/светлой темы:**

В проекте темы переключаются через атрибут `data-theme` на `body`:

```html
<!-- Темная тема -->
<body data-theme="dark">
  ...
</body>

<!-- Светлая тема (или по умолчанию) -->
<body data-theme="light">
  ...
</body>
<!-- или просто -->
<body>
  ...
</body>
<!-- :root переопределяет светлую тему -->
```

Компонент `Input` использует CSS переменные, которые автоматически переключаются при смене темы - никакого JavaScript
кода не требуется!

**Особенности:**

- **Адаптивность**: При переключении темы все компоненты автоматически используют соответствующие цвета
- **Без JavaScript**: Темы управляются CSS переменными и атрибутом `data-theme`
- **Следовать архитектуре**: Как компонент `Button`, использовать `var(--*)` вместо хардкода цветов
- **Transitions**: Плавные переходы для `border-color`, `box-shadow` (не для `background`, чтобы избежать конфликтов при
  смене темы)

**Референс для стилизации:**

- `apps/web/src/components/common/button/Button.scss` - пример использования CSS переменных
- `apps/web/src/components/common/newsletter-form/NewsletterForm.scss` - текущий стиль input
- `apps/web/src/styles/themes/` - все переменные для тем

#### 5. Интеграция в NewsletterForm

Заменить:

```tsx
<input
  type='email'
  value={email}
  onChange={e => setEmail(e.target.value)}
  placeholder='your@email.com'
  className='newsletter-form__input'
  disabled={isLoading || status === 'success'}
/>
```

На:

```tsx
<Input
  type='email'
  value={email}
  onChange={e => setEmail(e.target.value)}
  placeholder='your@email.com'
  disabled={isLoading || status === 'success'}
  error={errorMessage || undefined}
/>
```

## ✅ Чеклист выполнения

### TODO → DOING

- [x] Задача проанализирована
- [x] Изучена структура компонента `Button` как референс
- [x] Изучен компонент `NewsletterForm` для понимания требований
- [x] План реализации готов
- [x] Зависимости проверены

### DOING → TESTING

- [x] Создана структура компонента (`Input.tsx`, `Input.scss`, `index.ts`)
- [x] Реализован базовый функционал (props, типы, рендеринг)
- [x] Реализованы состояния (error, disabled, focus)
- [x] Добавлена стилизация (SCSS)
- [x] Поддержка темной/светлой темы
- [x] Accessibility атрибуты добавлены
- [x] Компонент интегрирован в `NewsletterForm`
- [x] Локальное тестирование пройдено

### TESTING → CODEREVIEW & DOCS

- [x] Компонент работает во всех состояниях
- [x] Валидация в `NewsletterForm` работает корректно
- [x] Accessibility проверен (keyboard navigation, screen readers)
- [x] Темная/светлая тема работает
- [x] Адаптивность проверена (mobile, tablet, desktop)
- [x] Performance проверен (нет лишних ре-рендеров)
- [x] TypeScript ошибок нет

### CODEREVIEW & DOCS → DONE

- [x] Код соответствует стандартам проекта
- [x] JSDoc комментарии добавлены
- [x] Примеры использования в комментариях
- [x] Компонент экспортирован через `index.ts`
- [x] Задача полностью протестирована
- [x] Регрессий нет

## 🔗 Связанные задачи

- **Используется в**: PW-004 (NewsletterForm в модальном окне)
- **Блокирует**: Будущие задачи с формами (авторизация, обратная связь, комментарии)
- **Связана с**: PW-004 (верстка страницы "О проекте")

## 📝 Заметки и комментарии

### История изменений

- 2026-01-20: Задача создана
- 2026-01-21: Компонент реализован и интегрирован в NewsletterForm
- 2026-01-21: Добавлена поддержка размеров (sm, md, lg) для соответствия высоте кнопок
- 2026-01-21: Исправлена высота инпута (line-height: 1) для точного совпадения с Button

### Дополнительная информация

#### Референсы

- **Компонент Button**: `apps/web/src/components/common/button/` - пример архитектуры и стилизации
- **NewsletterForm**: `apps/web/src/components/common/newsletter-form/NewsletterForm.tsx` - место интеграции
- **Темы**: `apps/web/src/styles/themes/` - CSS переменные для цветов

#### Особенности реализации

1. **Расширение стандартных атрибутов**: Использовать `React.InputHTMLAttributes<HTMLInputElement>` для наследования
   всех стандартных HTML атрибутов
2. **ForwardRef**: Использовать `React.forwardRef` для поддержки ref на нативный input элемент
3. **Состояние ошибки**: Поддержка как строки (сообщение), так и boolean (визуальный индикатор)
4. **Label опционален**: Не все input нуждаются в label (например, в модальном окне может быть заголовок формы)
5. **Helper text**: Отдельный от error текст для подсказок пользователю

#### Возможные расширения (будущее)

- `size` prop (sm, md, lg) - как в Button
- `icon` prop - иконка слева/справа
- `clearButton` - кнопка очистки значения
- `passwordToggle` - переключатель видимости пароля
- Валидация на уровне компонента (опционально)

#### Риски

- Низкий риск - компонент изолирован и не влияет на существующий функционал
- После интеграции в NewsletterForm нужно проверить, что валидация работает корректно

---

**Статусы**: TODO → DOING → TESTING → CODEREVIEW & DOCS → DONE
