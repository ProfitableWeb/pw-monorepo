# PW-023: Исправить форматтер для TSX в VSCode

## 📋 Информация о задаче

- **ID**: PW-023
- **Статус**: ✅ DONE
- **Создано**: 2026-02-10
- **Завершено**: 2026-02-10
- **Приоритет**: Low 📝
- **Компонент**: ⚙️ Tooling
- **Теги**: #tooling #vscode #formatter #prettier
- **Оценка времени**: ~5 минут

## 🎯 Постановка задачи

### Описание

В файле `.vscode/settings.json` для файлов `.tsx` (typescriptreact) настроен встроенный форматтер VSCode вместо
Prettier. Это создаёт несогласованность форматирования между `.ts` и `.tsx` файлами.

### Проблема

```json
"[typescriptreact]": {
  "editor.defaultFormatter": "vscode.typescript-language-features",  // ← встроенный
  "editor.formatOnSave": true,
  "editor.formatOnSaveMode": "file"
}
```

Для всех остальных типов используется Prettier:

```json
"[typescript]": {
  "editor.defaultFormatter": "esbenp.prettier-vscode"
}
```

### Цель

Использовать Prettier для всех типов файлов, включая `.tsx`, чтобы обеспечить согласованное форматирование во всём
проекте.

### Критерии приемки

- [x] Форматтер для `typescriptreact` изменён на `esbenp.prettier-vscode`
- [x] Убран `editor.formatOnSaveMode` (не нужен для Prettier)
- [x] Форматирование `.tsx` файлов работает корректно
- [x] Нет различий в форматировании между `.ts` и `.tsx`

## ✅ Чеклист выполнения

### TODO → DONE

- [x] Задача проанализирована
- [x] Изменён форматтер в `.vscode/settings.json`
- [x] Проверено форматирование `.tsx` файла

## 🔗 Связанные задачи

Отдельная задача, не блокирует другие.

## 📝 Заметки

### Изменения

**Было:**

```json
"[typescriptreact]": {
  "editor.defaultFormatter": "vscode.typescript-language-features",
  "editor.formatOnSave": true,
  "editor.formatOnSaveMode": "file"
}
```

**Станет:**

```json
"[typescriptreact]": {
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true
}
```

---

**Статусы**: TODO → DOING → TESTING → DONE
