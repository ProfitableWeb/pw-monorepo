# PW-024: Интеграция BlogDash в apps/admin

## 📋 Информация о задаче

- **ID**: PW-024
- **Статус**: ✅ DONE
- **Создано**: 2026-02-10
- **Завершено**: 2026-02-11
- **Приоритет**: High 🔥
- **Компонент**: ⚛️ Frontend (Admin)
- **Теги**: #admin #integration #tailwind #shadcn #vite
- **Оценка времени**: 30-60 минут
- **Фактическое время**: ~30 минут

## 🎯 Постановка задачи

### Описание

Интегрировать готовую админку из `F:\_GITHUB-APPS\BlogDash` в `apps/admin` монорепо.

**Исходник (BlogDash):**

- Vite 6 + React 18 (SPA)
- Tailwind CSS v4 + shadcn/ui + Radix UI (24+ компонентов)
- Zustand для state management
- Zustand-based роутинг (без react-router)
- 101 TypeScript/TSX файл, 50 shadcn/ui компонентов
- Дизайн из [Figma Make](https://www.figma.com/design/uNCNgquZOv719TpyNR1QGv/Blog-Management-Dashboard)

**Решение:**

Отказ от миграции на Next.js — сохранение оригинального стека Vite + React 18.

### Обоснование отказа от Next.js

Первая попытка миграции на Next.js 15 + React 19 привела к 16 fix-коммитам и неработающему результату. Основные
проблемы:

1. **React 18 → 19**: несовместимости типов во всех компонентах
2. **Vite → Next.js**: необходимость `'use client'` на каждом компоненте (админка полностью клиентская)
3. **Tailwind v4 → Next.js PostCSS**: конфликты конфигурации
4. **State-based роутинг → App Router**: бессмысленная переписка навигации

**Вывод**: Next.js не нужен для SPA-админки — нет SSR, нет SEO, нет серверных компонентов. Vite быстрее в dev-режиме и
проще в настройке.

Лог неудачной сессии:
[unsuccessful-session-PW-024-admin-integration.md](./unsuccessful-session-PW-024-admin-integration.md)

### Критерии приемки

- [x] BlogDash перенесён в `apps/admin` as-is
- [x] `package.json` адаптирован для монорепо (`@profitable-web/admin`)
- [x] MUI/emotion зависимости убраны (не использовались)
- [x] Dev-сервер на порту 3001
- [x] `bun run build` проходит без ошибок
- [x] `bun turbo dev --filter=@profitable-web/admin` работает
- [x] UI идентичен оригиналу BlogDash

## 📋 Выполненные шаги

### 1. Перенос файлов

- Удалён скелет Next.js из `apps/admin` (layout.tsx, page.tsx, next.config)
- Скопировано содержимое BlogDash → `apps/admin/`

### 2. Адаптация конфигов

- `package.json`: имя `@profitable-web/admin`, react из peer → dependencies, убраны MUI/emotion/@popperjs
- `vite.config.ts`: порт 3001
- Удалены `package-lock.json` (bun) и `postcss.config.mjs` (Tailwind v4 через Vite плагин)

### 3. Проверка

- `bun install` — ✅
- `bun run build` — ✅ (10 сек, 0 ошибок)
- `bun run dev` — ✅ (localhost:3001)

## 🔗 Связанные задачи

- **Блокируется**: PW-022 (дизайн админки завершен)
- **Блокирует**: Интеграция с backend API (будущие задачи)

## 📝 Заметки

### Убранные зависимости

- `@mui/material`, `@mui/icons-material` — установлены, но не использовались (0 импортов)
- `@emotion/react`, `@emotion/styled` — зависимости MUI
- `@popperjs/core` — не нужен напрямую (Radix UI имеет свой positioning)

### Техдолг

- Дублирование UI систем (Tailwind в admin, SCSS в web) — осознанное решение
- Chunk size warning при build (1.5MB) — настроить code splitting позже
- `@monaco-editor/react` в devDeps — перенести в deps или вынести за lazy import

---

**Статусы**: TODO → DOING → ~~TESTING~~ → DONE
