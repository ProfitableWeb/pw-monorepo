# PW-069: Уязвимости зависимостей (Dependabot)

## 📋 Информация о задаче

- **ID**: PW-069
- **Статус**: DOING (этап 1 выполнен)
- **Создано**: 2026-09-25
- **Приоритет**: High ⚡
- **Компонент**: 🔧 DevOps + 🐍 API + ⚛️ Web + ⚙️ Admin
- **Теги**: #security #dependencies #dependabot

## 🎯 Постановка задачи

### Описание

GitHub сообщает об открытых алертах Dependabot. Аудит локально (`bun audit`, `pip-audit` по `uv export --no-dev`) на
2026-09-25:

| Экосистема | Было                                                           | После этапа 1                                         |
| ---------- | -------------------------------------------------------------- | ----------------------------------------------------- |
| JS         | 128 (4 critical, 59 high, 54 moderate, 11 low) в 29 пакетах    | 20 пакетов, **все — сборка/dev или admin-бандл**      |
| Python     | 15 пакетов (starlette, python-multipart, cryptography, pyjwt…) | 1 — `ecdsa` (нет исправления, приходит с python-jose) |

### Этап 1 — выполнен (обновления в пределах semver, без мажорных версий)

**Web (прод-рантайм):**

- `next` 16.1.6 → **16.3.6** — 30 уязвимостей, в т.ч. critical (DoS Server Components, обход middleware)
- `dompurify` 3.3.1 → **3.4.16** — 18 уязвимостей санитайзера (им чистится HTML статей)
- `react`/`react-dom` 19.3, `shiki`, `@tanstack/react-query`, `sass`, `vitest` 3.2.7 и др.

**Admin:** tiptap 3.20 → **3.31.3** (ReDoS в markdown, `__proto__` в `mergeAttributes`), react 19.3.

**API:** `starlette` 0.50 → 1.7 (вместе с `fastapi` 0.128 → 0.141), `python-multipart` 0.0.21 → 0.0.32 (разбор
загрузок), `cryptography` 46 → 50, `pyjwt`, `pillow` 12.3, `urllib3`, `idna`, `anyio`, `pyasn1`, `mako`,
`pydantic-settings`, `python-dotenv`, `uvicorn`. `mcp` 1.26 → **1.28.1** — ограничен `<2` в `pyproject.toml`:
`uv lock --upgrade` пытался поднять до 2.2.0 (мажор, возможны изменения API FastMCP).

**Проверка:** pytest 32/32, ruff; lint + type-check + test web/admin; `next build` и `vite build` — успешно;
`bun install --frozen-lockfile` на bun **1.2.17** (версия в CI и Dockerfile) принимает lock-файл, записанный bun 1.3.

### Этап 2 — осталось

| Пакет                                                                                      | Откуда                                                          | Где работает                                  | Решение                                                                                                                            |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `ecdsa` (PYSEC-2026-1325, фикса нет)                                                       | `python-jose` — проект не поддерживается                        | API, JWT                                      | **Мигрировать JWT на `pyjwt`** (уже в зависимостях): `auth/jwt.py`, `auth/dependencies.py`, `api/auth.py`, `middleware/logging.py` |
| `dompurify` 3.2.7                                                                          | `monaco-editor` 0.55.1 (жёсткий пин)                            | admin-бандл (редактор кода)                   | обновить `monaco-editor`/`@monaco-editor/react` или `overrides`                                                                    |
| `lodash` 4.17.23                                                                           | `recharts` 2.15.2 (пин в admin)                                 | admin-бандл (графики)                         | recharts 2.x последний патч или 3.x (мажор)                                                                                        |
| `vite` 6.3.5, `rollup`, `postcss`, `nanoid`, `tar`                                         | точные пины в `apps/admin/package.json` (vite, tailwind 4.1.12) | только сборка (admin в проде — nginx-статика) | снять точные пины → `^`, обновить vite до 6.4+/7, tailwind 4.x                                                                     |
| `ws`, `flatted`, `picomatch`, `minimatch`, `brace-expansion`, `yaml`, `browserslist` и др. | транзитивные dev-зависимости (eslint, jsdom, vitest)            | только dev/CI                                 | обновить lock-файл целиком (`rm bun.lock && bun install`) после снятия пинов                                                       |
| `vitest` 3.x (moderate)                                                                    | web devDependency                                               | только dev                                    | vitest 4 (мажор)                                                                                                                   |

### Этап 3 — предотвращение

- [ ] `.github/dependabot.yml`: еженедельные PR для `npm` (корень) и `pip` (`apps/api`), группировка minor/patch
- [ ] Шаг аудита в CI (`bun audit --audit-level=high` только для прод-зависимостей, `pip-audit`) — сначала как
      информационный, потом блокирующий
- [ ] CLAUDE.md: bun в `packageManager` (1.2.17) и локальный bun расходятся — зафиксировать версию (`.bun-version` или
      обновить `packageManager` и Dockerfile согласованно)

## ✅ Чеклист выполнения

- [x] Этап 1: обновления в пределах semver + проверка сборок
- [ ] Миграция python-jose → pyjwt
- [ ] Admin: monaco, recharts, снятие точных пинов, vite/tailwind
- [ ] Полное обновление транзитивных зависимостей
- [ ] Dependabot config + аудит в CI
- [ ] KANBAN
