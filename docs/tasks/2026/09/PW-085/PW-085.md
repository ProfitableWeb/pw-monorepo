# PW-085: Админка с моков на API — фаза 1: календарь публикаций и автопубликация

> Заведена облачной сессией 25.09 как PW-068; номер был занят в develop, перенумерована при слиянии master → develop. В
> истории коммитов — PW-068.

## 📋 Информация о задаче

- **ID**: PW-085
- **Статус**: DOING (часть 1 — автопубликация — готова)
- **Создано**: 2026-09-25
- **Приоритет**: High ⚡
- **Компонент**: 🐍 API + ⚙️ Admin
- **Теги**: #admin #calendar #scheduling #mock-to-api

## 🎯 Постановка задачи

### Контекст: карта моков в админке

| Раздел                                                                   | Статус                                                             |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------ |
| articles, categories, tags, pages, media, mcp, users, settings           | ✅ реальный API                                                    |
| dashboard                                                                | частично: `AnalyticsChart.tsx` — хардкод `data = [...]`            |
| seo                                                                      | частично: fallback на `MOCK_*`; база знаний — полностью мок        |
| article-workbench                                                        | частично: `ProvenanceModule` (`MOCK_WORKSPACES`), `EDITORIAL_TEAM` |
| **calendar**                                                             | мок (`calendar.constants.ts` → `mockScheduledPosts`)               |
| research, ai-center, ads, analytics, promotion, style, manifest, socials | мок                                                                |

Эта задача — **фаза 1**: календарь. Он ближе всего к ядру блога (публикации), и данные для него уже есть в БД. Порядок
следующих фаз — в конце документа.

### 🔥 Обнаружено: запланированные статьи никогда не публикуются

`schedule_article()` (`services/articles/admin.py:301`) и MCP-инструмент `schedule_article` ставят `status = SCHEDULED`
и `published_at` в будущем. MCP-ресурс даже описывает статус как «опубликуется автоматически».

Но **механизма автопубликации нет**: ни планировщика, ни фоновой задачи, а публичные запросы
(`services/articles/queries.py`) фильтруют строго `status == PUBLISHED`. Запланированная статья навсегда остаётся
невидимой для читателей, sitemap и RSS.

Календарь без работающего планирования — бессмысленный, поэтому автопубликация входит в эту задачу первой.

### Цель

1. Статьи в статусе `scheduled` публикуются в назначенное время без участия человека.
2. Календарь админки показывает реальные опубликованные и запланированные статьи и позволяет планировать.

### Часть 1: Автопубликация (API)

**Решение принято и реализовано** — [ADR-005](../../../../architecture/decisions/ADR-005-scheduled-publishing.md):
фоновая asyncio-задача в `lifespan` раз в 60 с вызывает `publish_due_articles()`
(`src/services/articles/scheduling.py`). Условный `UPDATE ... WHERE status = 'scheduled'` на каждую статью — безопасно
при нескольких воркерах; audit log `article.auto_publish`; флаг `ARTICLE_PUBLISHER_ENABLED`.

**Критерии:**

- [x] ADR в `docs/architecture/decisions/` — ADR-005
- [x] Автопубликация работает; тесты — `tests/test_scheduling.py` (только наступившие, идемпотентность, видимость в
      публичном API)
- [x] Тик не падает при недоступной БД (лог + следующий тик) — тест + проверено запуском без БД
- [x] Статья появляется на сайте без ручных действий (страницы `force-dynamic`); sitemap и RSS — с задержкой до 1 ч
      (`revalidate = 3600`), принято в ADR-005
- [ ] Отмена планирования: `scheduled → draft` из админки

### Часть 2: Календарь (API + Admin)

**API.** Эндпоинт `GET /api/admin/articles/calendar?from=&to=` — статьи со статусами `published` и `scheduled` с
`published_at` в диапазоне; ответ — `id, title, slug, status, published_at, primary_category{name,slug,color}`. Лёгкая
схема, без контента. Альтернатива — существующий `GET /api/admin/articles` с фильтрами `date_from/date_to` (решить при
реализации; отдельный эндпоинт проще и быстрее).

**Admin.**

- `useCalendarPosts(range)` в `hooks/api/`, маппинг в `ScheduledPost` (`calendar.types.ts`)
- `mockScheduledPosts` удалить; тип `ai-suggestion` пока не показывать (нет источника данных — это часть AI-центра)
- Клик по посту → редактор статьи (navigation-store)
- Drag & drop поста на другой день (если в видах календаря он есть; иначе — диалог «перенести») →
  `POST /articles/{id}/schedule`
- «Запланировать» из календаря: выбор черновика + дата/время → schedule
- Таймзона: `SystemSettings.timezone` уже есть (смещение вида `+03:00`, не IANA — переходы на летнее время не
  учитываются; для России допустимо) — отображать в ней, отправлять в UTC

**Критерии:**

- [ ] Календарь показывает реальные статьи за видимый период (месяц/неделя)
- [ ] Перенос даты работает, прошлое запрещено
- [ ] Время корректно с учётом `SystemSettings.timezone`
- [ ] `calendar.constants.ts` без моков, README раздела обновлён
- [ ] Тесты API для calendar-эндпоинта

### Следующие фазы (отдельные задачи, предлагаемый порядок)

1. **Dashboard** — `AnalyticsChart` на Яндекс.Метрику (прокси уже есть, PW-048); проверить, что `BlogStats` считает все
   статьи через admin-хуки, а не только опубликованные через публичные.
2. **SEO** — убрать fallback на `MOCK_*` (показывать ошибку/пустое состояние); база знаний — модель + CRUD.
3. **Article workbench** — `EDITORIAL_TEAM` из `/admin/users`; `ProvenanceModule` — после исследований.
4. **Research / AI-center** — самые крупные; требуют отдельного проектирования (хранение сессий, провайдеры LLM).
5. **ads, analytics, promotion, style, manifest, socials** — пересмотреть: какие разделы реально нужны блогу сейчас;
   ненужные — скрыть из навигации до реализации, чтобы админка не показывала фейковые цифры.

## ✅ Чеклист выполнения

- [x] ADR автопубликации
- [x] Автопубликация + тесты
- [x] Проверка кеша web
- [ ] Calendar API + тесты
- [ ] Календарь в админке
- [ ] KANBAN
