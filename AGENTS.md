## Learned User Preferences

- Keep Cursor rules short; split broad guidance into several narrow files when a rule starts getting large.
- When reusable templates cover code context, structure them for both frontend TypeScript and backend Python instead of
  treating TypeScript as the only case.

## Learned Workspace Facts

- ProfitableWeb uses GitVerse `origin` as the primary repository and GitHub `github` as a mirror.
- Agent-oriented development documentation lives in `docs/agentic-development.md`, with context-header templates split
  under `docs/templates/frontend`, `docs/templates/backend`, and `docs/templates/shared`.
