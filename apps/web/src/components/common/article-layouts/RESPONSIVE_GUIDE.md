# Руководство по работе с адаптивностью

## Быстрый старт

### Хочу изменить размеры колонок на Desktop

→ Открой `ArticleLayoutThreeColumn/_responsive-layout.scss` → Найди секцию `// DESKTOP (1200-1600px)` → Измени
`grid-template-columns: 220px 1fr 300px;`

### Хочу изменить отступы на Mobile

→ Открой `ArticleLayoutThreeColumn/_responsive-sizing.scss` → Найди секцию `// MOBILE (<640px)` → Измени
`padding: 40px 20px 80px;`

### Хочу изменить размер шрифта на Tablet

→ Открой `article-content/_responsive-typography.scss` → Найди секцию `// TABLET (900-1200px)` → Измени
`font-size: 18px;`

## Типы файлов

### 📐 \_responsive-layout.scss

**Что здесь:** Структурные изменения layout'а

**Какие свойства:**

- `grid-template-columns` - количество и размер колонок
- `display` - видимость элементов (block/none)
- `position` - позиционирование (sticky/static)
- `max-width` - ограничения ширины
- `margin` - центрирование элементов

**Когда менять:**

- Хочу добавить/убрать колонку на определенном разрешении
- Хочу изменить ширину sidebar или TOC
- Хочу изменить, когда колонки схлопываются
- Хочу изменить видимость элементов

**Пример изменения:**

```scss
// Было: TOC появляется с 1200px
@media (min-width: 1200px) {
  &__toc {
    display: block;
  }
}

// Стало: TOC появляется раньше, с 1024px
@media (min-width: 1024px) {
  &__toc {
    display: block;
  }
}
```

### 📏 \_responsive-sizing.scss

**Что здесь:** Размеры и отступы

**Какие свойства:**

- `gap` - расстояние между колонками в grid
- `padding` - внутренние отступы контейнера
- `margin` - внешние отступы
- `width`/`height` - фиксированные размеры

**Когда менять:**

- Хочу увеличить/уменьшить расстояние между колонками
- Хочу изменить отступы от краев экрана
- Хочу настроить spacing для разных разрешений

**Пример изменения:**

```scss
// Было: gap 60px на Desktop
@media (min-width: 1200px) {
  .article-layout-three-column {
    gap: 60px;
  }
}

// Стало: gap 80px для более воздушного layout'а
@media (min-width: 1200px) {
  .article-layout-three-column {
    gap: 80px;
  }
}
```

### 📝 \_responsive-typography.scss

**Что здесь:** Типографика контента

**Какие свойства:**

- `font-size` - размер шрифта
- `line-height` - межстрочный интервал

**Когда менять:**

- Хочу увеличить/уменьшить размер текста
- Хочу изменить читаемость (line-height)
- Хочу настроить типографику для разных устройств

**Пример изменения:**

```scss
// Было: 17px на Desktop
@media (min-width: 1200px) {
  .article-content {
    font-size: 17px;
    line-height: 1.6;
  }
}

// Стало: 18px для лучшей читаемости
@media (min-width: 1200px) {
  .article-content {
    font-size: 18px;
    line-height: 1.65;
  }
}
```

### 📐 \_responsive-spacing.scss

**Что здесь:** Отступы между элементами контента

**Какие свойства:**

- `margin-bottom` - отступ между параграфами
- `margin-top` - отступ перед заголовками
- `margin` - отступы для цитат, изображений, таблиц

**Когда менять:**

- Хочу увеличить/уменьшить расстояние между параграфами
- Хочу изменить отступы между секциями
- Хочу настроить spacing для разных разрешений

**Пример изменения:**

```scss
// Было: 1.3em между параграфами на Desktop
@media (min-width: 1200px) {
  .article-content {
    p {
      margin-bottom: 1.3em;
    }
  }
}

// Стало: 1.5em для более воздушного layout'а
@media (min-width: 1200px) {
  .article-content {
    p {
      margin-bottom: 1.5em;
    }
  }
}
```

## Структура брейкпоинтов

Все файлы используют единую систему брейкпоинтов:

```scss
// MOBILE (<640px) - базовые стили без media query
.selector {
  // стили для mobile
}

// TABLET SMALL (640-900px)
@media (min-width: 640px) { ... }

// TABLET (900-1200px)
@media (min-width: 900px) { ... }

// DESKTOP (1200-1600px)
@media (min-width: 1200px) { ... }

// WIDE DESKTOP (>1600px)
@media (min-width: 1600px) { ... }
```

## Частые сценарии

### Сценарий 1: Sidebar появляется слишком поздно

**Проблема:** Хочу, чтобы sidebar появлялся раньше (с 768px вместо 900px)

**Решение:**

1. Открой `ArticleLayoutThreeColumn/_responsive-layout.scss`
2. Найди секцию `// TABLET (900-1200px)`
3. Измени `@media (min-width: 900px)` на `@media (min-width: 768px)`
4. Открой `ArticleLayoutThreeColumn/_responsive-sizing.scss`
5. Найди ту же секцию и измени там тоже

### Сценарий 2: Слишком узкий контент на Tablet

**Проблема:** На планшете контент ограничен 720px, хочу 800px

**Решение:**

1. Открой `ArticleLayoutThreeColumn/_responsive-layout.scss`
2. Найди секцию `// TABLET SMALL (640-900px)`
3. Измени `max-width: 720px;` на `max-width: 800px;`

### Сценарий 3: Мелкий шрифт на Mobile

**Проблема:** Шрифт 16px слишком мелкий, хочу 17px

**Решение:**

1. Открой `article-content/_responsive-typography.scss`
2. Найди базовый стиль (без media query)
3. Измени `font-size: 16px;` на `font-size: 17px;`

### Сценарий 4: Новый брейкпоинт для больших планшетов

**Проблема:** Хочу особое поведение для 1024-1200px

**Решение:**

1. Открой нужный файл (layout или sizing)
2. Добавь новую секцию:

```scss
// ============================================================
// LARGE TABLET (1024-1200px) - особое поведение
// ============================================================
@media (min-width: 1024px) and (max-width: 1199px) {
  .article-layout-three-column {
    // твои настройки
  }
}
```

## Советы

✅ **DO:**

- Всегда добавляй комментарий с описанием брейкпоинта
- Меняй одновременно в layout и sizing для консистентности
- Используй единицы `px` для брейкпоинтов (проще debug)
- Тестируй на реальных устройствах, не только в DevTools

❌ **DON'T:**

- Не смешивай структуру и размеры в одном файле
- Не используй слишком много брейкпоинтов (3-5 достаточно)
- Не забывай про performance (минимизируй сложность)
- Не пренебрегай mobile-first подходом

## Тестирование

После изменений проверь на разных разрешениях:

- 375px (iPhone SE)
- 768px (iPad portrait)
- 1024px (iPad landscape)
- 1280px (Laptop)
- 1920px (Desktop)

Используй Chrome DevTools: `Cmd/Ctrl + Shift + M`
