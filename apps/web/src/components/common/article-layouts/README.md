# Article Layouts - Универсальная система layout'ов

## Структура файлов

Адаптивность разделена на два типа файлов для максимальной фокусировки:

```
article-layouts/
├── ArticleLayoutThreeColumn/
│   ├── ArticleLayoutThreeColumn.tsx
│   ├── ArticleLayoutThreeColumn.scss     # Базовые стили (цвета, scrollbar)
│   ├── _responsive-layout.scss           # ⚡ Структура (grid, display, position)
│   ├── _responsive-sizing.scss           # 📏 Размеры (gap, padding, margin)
│   └── index.ts
│
├── ArticleLayoutTwoColumn/
│   ├── ArticleLayoutTwoColumn.tsx
│   ├── ArticleLayoutTwoColumn.scss       # Базовые стили
│   ├── _responsive-layout.scss           # ⚡ Структура
│   ├── _responsive-sizing.scss           # 📏 Размеры
│   └── index.ts
│
├── ArticleLayoutFullWidth/
│   ├── ArticleLayoutFullWidth.tsx
│   ├── ArticleLayoutFullWidth.scss       # Базовые стили
│   ├── _responsive-layout.scss           # ⚡ Структура (вспом. классы)
│   ├── _responsive-sizing.scss           # 📏 Размеры (padding)
│   └── index.ts
│
└── article-content/
    ├── ArticleContent.tsx
    ├── ArticleContent.scss               # Базовая типографика
    ├── _responsive-typography.scss       # 📝 Типографика (font-size, line-height)
    └── _responsive-spacing.scss          # 📐 Отступы (margin между элементами)
```

### Зачем такое детальное разделение?

**Максимальная фокусировка на одном типе изменений:**

- **\_responsive-layout.scss**: Меняете структуру? Открываете только этот файл.
  - grid-template-columns
  - display
  - position
  - visibility элементов

- **\_responsive-sizing.scss**: Настраиваете отступы? Только этот файл.
  - gap
  - padding
  - margin
  - width/height

- **\_responsive-typography.scss**: Работаете с шрифтами? Только этот файл.
  - font-size
  - line-height

**Преимущества:**

- 🎯 **Фокус**: не нужно скроллить через media queries с разными типами изменений
- 🧹 **Чистота**: каждый файл отвечает только за свой аспект
- 📖 **Читаемость**: сразу понятно, где что искать
- ⚡ **Скорость**: быстрее найти и изменить нужный параметр

## Брейкпоинты

Оптимизированы для максимальной читабельности контента на всех разрешениях:

### Mobile (<640px)

- **Layout**: 1 колонка
- **Контент**: полная ширина с padding
- **TOC**: скрыт
- **Sidebar**: скрыт
- **Размер шрифта**: 16px

### Tablet Small (640-900px)

- **Layout**: 1 колонка с ограничением max-width
- **Контент**: max-width 720px (центрирован)
- **TOC**: скрыт
- **Sidebar**: скрыт
- **Размер шрифта**: 17px
- **Цель**: оптимальная ширина строки для читабельности на планшетах в портретной ориентации

### Tablet (900-1200px)

- **Layout**: 2 колонки (Content | Sidebar)
- **Контент**: расширяется автоматически в grid
- **TOC**: скрыт
- **Sidebar**: показан (280px)
- **Размер шрифта**: 18px
- **Цель**: появление дополнительной информации в сайдбаре без потери читабельности

### Desktop (1200-1600px)

- **Layout**: 3 колонки (TOC | Content | Sidebar)
- **Контент**: расширяется автоматически в grid
- **TOC**: показан (220px)
- **Sidebar**: показан (300px)
- **Размер шрифта**: 18px
- **Цель**: полноценное отображение всех элементов

### Wide Desktop (>1600px)

- **Layout**: 3 колонки с увеличенными отступами
- **Контент**: расширяется с ограничением max-width контейнера (1400px)
- **TOC**: показан (220px)
- **Sidebar**: показан (300px)
- **Gap**: 80px между колонками
- **Цель**: комфортное чтение на больших мониторах

## Оптимальная ширина строки

Для максимальной читабельности применяется `max-width: 75ch` для параграфов и списков. Это обеспечивает оптимальные
65-75 символов в строке, рекомендованные для длинных текстов.

## Использование

```tsx
// Three Column Layout (TOC + Content + Sidebar)
<ArticleLayout
  layout="three-column"
  toc={<TableOfContents items={tocItems} />}
  sidebar={<AuthorCard />}
>
  <ArticleContent html={content} />
</ArticleLayout>

// Two Column Layout (Content + Sidebar)
<ArticleLayout
  layout="two-column"
  sidebar={<RelatedArticles />}
>
  <ArticleContent html={content} />
</ArticleLayout>

// Full Width Layout (только Content)
<ArticleLayout layout="full-width">
  <ArticleContent html={content} />
</ArticleLayout>
```

## Схема схлопывания колонок

```
<640px:     [Content]
640-900px:  [Content (max-width 720px)]
900-1200px: [Content | Sidebar]
1200-1400px: [Content | Sidebar]
>1400px:    [TOC | Content | Sidebar]
```

Колонки схлопываются поэтапно для сохранения оптимальной ширины контента:

1. Сначала исчезает TOC (<1400px) - требуется минимум 1400px для комфортного 3-колоночного режима
2. Затем исчезает Sidebar (<900px)
3. На узких экранах контент центрируется с ограничением ширины (640-900px)
4. На мобильных контент занимает всю ширину (<640px)

## Настройка адаптивности

### Где что менять

**Структура layout (колонки, видимость):**

- Three Column: `ArticleLayoutThreeColumn/_responsive-layout.scss`
- Two Column: `ArticleLayoutTwoColumn/_responsive-layout.scss`
- Full Width: `ArticleLayoutFullWidth/_responsive-layout.scss`

**Размеры (отступы, gap):**

- Three Column: `ArticleLayoutThreeColumn/_responsive-sizing.scss`
- Two Column: `ArticleLayoutTwoColumn/_responsive-sizing.scss`
- Full Width: `ArticleLayoutFullWidth/_responsive-sizing.scss`

**Типографика (шрифты):**

- Контент: `article-content/_responsive-typography.scss`

### Примеры файлов

**\_responsive-layout.scss** - только структура:

```scss
// ============================================================
// TABLET (900-1200px) - Content | Sidebar (TOC скрыт)
// ============================================================
@media (min-width: 900px) {
  .article-layout-three-column {
    grid-template-columns: 1fr 280px; // Структура grid

    &__toc {
      display: none; // Видимость TOC
    }

    &__sidebar {
      display: block; // Видимость Sidebar
      position: sticky; // Позиционирование
    }
  }
}
```

**\_responsive-sizing.scss** - только размеры:

```scss
// ============================================================
// TABLET (900-1200px)
// ============================================================
@media (min-width: 900px) {
  .article-layout-three-column {
    gap: 50px; // Расстояние между колонками
    padding: 40px 30px 80px; // Внутренние отступы
  }
}
```

**\_responsive-typography.scss** - только типографика:

```scss
// ============================================================
// TABLET (900-1200px)
// ============================================================
@media (min-width: 900px) {
  .article-content {
    font-size: 18px; // Размер шрифта
    line-height: 1.8; // Межстрочный интервал
  }
}
```
