name: Deploy to Cloud.ru (GitVerse)

on:
  push:
    branches:
      - master
    paths:
      - "apps/**"
      - "packages/**"
      - "ecosystem.config.js"
      - ".gitverse/workflows/deploy.yaml"
      - "infra/nginx/**"
      - "infra/scripts/**"
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY || variables.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.SSH_HOST || variables.SSH_HOST }} >> ~/.ssh/known_hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY || variables.SSH_PRIVATE_KEY }}
          SSH_HOST: ${{ secrets.SSH_HOST || variables.SSH_HOST }}

      - name: Write API env file
        run: |
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -p ${SSH_PORT:-22} \
              ${SSH_USER}@${SSH_HOST} "cat > ~/profitableweb/apps/api/.env" << ENVEOF
          DATABASE_URL=${DATABASE_URL}
          JWT_SECRET=${JWT_SECRET}
          YANDEX_CLIENT_ID=${YANDEX_CLIENT_ID}
          YANDEX_CLIENT_SECRET=${YANDEX_CLIENT_SECRET}
          GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
          GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
          TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
          FRONTEND_URL=${FRONTEND_URL}
          ADMIN_URL=${ADMIN_URL}
          CORS_ORIGINS=${CORS_ORIGINS}
          ENVEOF
        env:
          SSH_USER: ${{ secrets.SSH_USER || variables.SSH_USER }}
          SSH_HOST: ${{ secrets.SSH_HOST || variables.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT || variables.SSH_PORT }}
          DATABASE_URL: ${{ secrets.DATABASE_URL || variables.DATABASE_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || variables.JWT_SECRET }}
          YANDEX_CLIENT_ID: ${{ secrets.YANDEX_CLIENT_ID || variables.YANDEX_CLIENT_ID }}
          YANDEX_CLIENT_SECRET: ${{ secrets.YANDEX_CLIENT_SECRET || variables.YANDEX_CLIENT_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID || variables.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET || variables.GOOGLE_CLIENT_SECRET }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || variables.TELEGRAM_BOT_TOKEN }}
          FRONTEND_URL: ${{ secrets.FRONTEND_URL || variables.FRONTEND_URL }}
          ADMIN_URL: ${{ secrets.ADMIN_URL || variables.ADMIN_URL }}
          CORS_ORIGINS: ${{ secrets.CORS_ORIGINS || variables.CORS_ORIGINS }}

      - name: Deploy to VM
        run: |
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -p ${SSH_PORT:-22} \
              ${SSH_USER}@${SSH_HOST} << 'EOF'
            set -e
            export PATH="$HOME/.local/bin:$HOME/.bun/bin:$PATH"

            # Клонирование или обновление репозитория
            if [ -d ~/profitableweb/.git ]; then
              echo "📥 Обновление репозитория..."
              cd ~/profitableweb
              git fetch origin
              git reset --hard origin/master
            else
              echo "📦 Клонирование репозитория..."
              rm -rf ~/profitableweb
              git clone https://gitverse.ru/profitableweb.ru/pw-monorepo.git ~/profitableweb
              cd ~/profitableweb
            fi

            # Установка JS-зависимостей из корня монорепо (единый lockfile)
            bun install

            # API: установка зависимостей + миграции БД
            cd apps/api
            uv sync
            uv run alembic upgrade head

            # Web: проверка типов + сборка
            cd ../web
            bun run type-check
            bun run build

            # Admin: сборка
            cd ../admin
            bun run build

            # Перезапуск всех сервисов
            cd ~/profitableweb
            pm2 restart ecosystem.config.js --update-env || pm2 start ecosystem.config.js

            echo "✅ Deploy completed!"
          EOF
        env:
          SSH_USER: ${{ secrets.SSH_USER || variables.SSH_USER }}
          SSH_HOST: ${{ secrets.SSH_HOST || variables.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT || variables.SSH_PORT }}

      - name: Verify deployment
        run: |
          echo "🔍 Проверка доступности сервиса..."
          sleep 5
          curl -f http://${{ secrets.SSH_HOST || variables.SSH_HOST }}/api/health || echo "⚠️ Health check failed, but deploy might still be running"
